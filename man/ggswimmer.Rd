% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/swimmer.R
\name{ggswimmer}
\alias{ggswimmer}
\title{Draw an interactive swimmer plot}
\usage{
ggswimmer(
  data,
  id,
  end,
  events = NULL,
  group = NULL,
  ongoing = NULL,
  start = NULL,
  hover = NULL,
  sort = c("duration", "group", "response", "data"),
  palette = NULL,
  ongoing_label = "Ongoing",
  xlab = "Time",
  legend = TRUE,
  title = NULL,
  caption = NULL,
  family = "Lato"
)
}
\arguments{
\item{data}{A data frame with one row per patient.}

\item{id}{Column of \code{data} identifying the patient.}

\item{end}{Column of \code{data}, or an expression of its columns, giving the
time each bar ends.}

\item{events}{Optional data frame with one row per event: the patient's
id in a column named as \code{id} is in \code{data}, the time in \code{time} and what
happened in \code{event}.}

\item{group}{Optional column of \code{data}, such as the arm, that colors the
bars.}

\item{ongoing}{Optional logical column of \code{data}, or an expression of its
columns, marking patients still ongoing at \code{end}, drawn with an arrow.}

\item{start}{Optional column of \code{data} giving the time each bar starts.
Defaults to zero.}

\item{hover}{Names of columns of \code{data} shown in each lane's hover card.}

\item{sort}{Order of the lanes in a static copy and when the widget
opens: \code{"duration"}, the longest first; \code{"group"}; \code{"response"}, those
with a complete response first, then a partial one; or \code{"data"}, the
order of \code{data}.}

\item{palette}{Colors for the groups, unnamed in level order or named by
group.}

\item{ongoing_label}{What the arrow means, for the legend and the hover
card, such as \code{"On treatment"} or \code{"Alive at last follow-up"}.}

\item{xlab}{Axis label. Include the unit, such as \code{"Months since first dose"}.}

\item{legend}{Draw a legend of the groups and events above the plot.}

\item{title, caption}{Title above the plot and note below it.}

\item{family}{Font family. The package ships Lato and registers it on load.}
}
\value{
An object of class \code{ggswimmer}, which prints as an interactive
widget. Use \code{\link[=graph_widget]{graph_widget()}}, \code{\link[=graph_plot]{graph_plot()}} or \code{\link[=graph_save]{graph_save()}} for the
widget, a static ggplot or a file.
}
\description{
Draws one lane per patient: a bar for the time on treatment or on study,
marks for the events along it, such as responses, progression and death,
and an arrow for patients still ongoing. Hovering over a lane shows the
patient's group, the columns named in \code{hover}, the first time of each
event and whether they are ongoing, and fades the other lanes. Clicking
it opens the patient's events in order under the plot, beside their full
record. Buttons under the widget reorder the lanes, which slide into
place.
}
\details{
Events are drawn by their wording: a complete response as a star, a
partial response or other response as a triangle, progression, relapse or
recurrence as a diamond, and death as a cross, with any other event as a
circle or square in its own color.
}
\examples{
if (requireNamespace("survival", quietly = TRUE)) {
  aml <- subset(survival::myeloid, id <= 30)
  month <- 30.44
  events <- rbind(
    data.frame(id = aml$id, time = aml$crtime / month, event = "Complete response"),
    data.frame(id = aml$id, time = aml$txtime / month, event = "Transplant"),
    data.frame(id = aml$id, time = aml$rltime / month, event = "Relapse"),
    data.frame(id = aml$id, time = ifelse(aml$death == 1, aml$futime / month, NA),
               event = "Death")
  )
  events <- events[!is.na(events$time), ]
  ggswimmer(aml, id, futime / month, events = events, group = trt,
            ongoing = death == 0, hover = c("sex", "flt3"),
            ongoing_label = "Alive at last follow-up",
            xlab = "Months since randomization")
}
}
