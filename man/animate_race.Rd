% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/race.R
\name{animate_race}
\alias{animate_race}
\title{Render a bar chart race to a file}
\usage{
animate_race(
  x,
  file = "race.mp4",
  loop = TRUE,
  cores = max(1L, parallel::detectCores() - 1L),
  quiet = FALSE
)
}
\arguments{
\item{x}{A \code{ggrace} object from \code{\link[=ggrace]{ggrace()}}.}

\item{file}{Output path, ending in \code{.gif} or \code{.mp4}.}

\item{loop}{Loop the GIF. Ignored for video.}

\item{cores}{Number of cores to draw frames on. Frames are independent, so
this scales close to linearly. Forced to 1 on Windows, where
\code{\link[parallel:mclapply]{parallel::mclapply()}} cannot fork.}

\item{quiet}{Suppress the progress bar. Progress is not reported when
drawing on more than one core.}
}
\value{
\code{file}, invisibly.
}
\description{
Draws every frame with 'ragg' and encodes them. The encoder follows the
file extension: \code{.gif} uses 'gifski' and falls back to 'magick', anything
else is treated as video and uses 'av', falling back to an \code{ffmpeg} binary
on the search path.
}
