% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/map.R
\name{animate_choropleth}
\alias{animate_choropleth}
\title{Animate a choropleth map}
\usage{
animate_choropleth(
  x,
  file = "map.gif",
  step = 0.6,
  end_pause = 2,
  fps = 20,
  res = 150,
  loop = TRUE,
  cores = max(1L, parallel::detectCores() - 1L),
  quiet = FALSE
)
}
\arguments{
\item{x}{A map from \code{\link[=ggchoropleth]{ggchoropleth()}}.}

\item{file}{Output path, ending in \code{.gif} or \code{.mp4}.}

\item{step}{Seconds from one time to the next.}

\item{end_pause}{Seconds to hold the final frame.}

\item{fps}{Frames per second.}

\item{res}{Output resolution in pixels per inch.}

\item{loop}{Loop the GIF. Ignored for video.}

\item{cores}{Number of cores to draw frames on.}

\item{quiet}{Suppress the progress bar.}
}
\value{
\code{file}, invisibly.
}
\description{
Plays the years of a \code{\link[=ggchoropleth]{ggchoropleth()}} map, easing each region from one
year's shade to the next, and writes the result to a GIF or MP4.
}
\examples{
\donttest{
m <- ggchoropleth(subset(clefts_qci_world, year >= 2015), iso3, year,
                  values = c(QCI = "qci"))
animate_choropleth(m, tempfile(fileext = ".gif"), step = 0.3, fps = 6,
                   res = 60, cores = 1)
}
}
