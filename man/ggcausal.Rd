% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/causal.R
\name{ggcausal}
\alias{ggcausal}
\title{Draw an interactive causal diagram}
\usage{
ggcausal(
  edges,
  nodes = NULL,
  direction = c("right", "down"),
  palette = NULL,
  legend = TRUE,
  legend_title = NULL,
  title = NULL,
  caption = NULL,
  family = "Lato"
)
}
\arguments{
\item{edges}{A data frame with one row per arrow and columns \code{from} and
\code{to} naming its ends. Optional columns: \code{rationale}, the reason for the
arrow and its direction, and \code{references}. Any other column is shown as a
labeled field.}

\item{nodes}{A data frame with one row per node and a \code{name} column
matching \code{from} and \code{to}. Optional columns: \code{label}, the text in the box,
which may contain line breaks and defaults to \code{name}; \code{role};
\code{rationale}; \code{references}; and \code{x} and \code{y} to place the box by hand, in
grid units with \code{y} pointing up. Any other column is shown as a labeled
field. When \code{NULL}, the nodes are taken from \code{edges}.}

\item{direction}{Which way the arrows point: \code{"right"} or \code{"down"}.}

\item{palette}{Colors for the roles, as a vector named by role. Roles left
out keep their default color.}

\item{legend}{Draw a legend of the roles above the diagram.}

\item{legend_title}{Text in front of the legend, such as \code{"Role"}.}

\item{title, caption}{Title above the diagram and note below it.}

\item{family}{Font family. The package ships Lato and registers it on load.}
}
\value{
An object of class \code{ggcausal}, which prints as an interactive
widget. Use \code{\link[=graph_widget]{graph_widget()}}, \code{\link[=graph_plot]{graph_plot()}} or \code{\link[=graph_save]{graph_save()}} for the
widget, a static ggplot or a file. The fields \code{width} and \code{height} give
the natural size in inches.
}
\description{
Draws a directed acyclic graph (DAG) in which every node and every arrow
carries its own justification. Hovering over a node shows its role and the
reason it is in the diagram; hovering over an arrow shows the assumed
direction of effect and the reason for it. Clicking either opens a panel
under the diagram with the full rationale and clickable references, which
also makes the diagram usable on touch screens.
}
\details{
The layout is layered, so every arrow points the same way, left to right
or top down. An arrow that skips a layer is routed around the boxes in
between on a smooth curve. Give \code{x} and \code{y} columns in \code{nodes} to place the
boxes yourself; arrows are then drawn straight.

Nodes are colored by \code{role}. The roles \code{exposure}, \code{outcome},
\code{confounder}, \code{mediator}, \code{collider}, \code{instrument}, \code{unobserved} and
\code{latent} have fixed colors, and the last two are drawn with a dashed
border. Any other role is allowed and takes the next color from
\code{\link[=race_palette]{race_palette()}}.

\code{references} may be a list column with one character vector per
row, or text with several references separated by \code{|} or a line break.
A reference that contains a URL is linked to it; otherwise one that
contains a DOI is linked to \verb{https://doi.org/}.
}
\examples{
dag <- ggcausal(cleft_dag$edges, cleft_dag$nodes, legend_title = "Role")
dag

# Only the arrows are required.
ggcausal(data.frame(from = c("A", "A", "B"), to = c("B", "C", "C")))
}
