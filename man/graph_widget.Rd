% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/graph.R
\name{graph_widget}
\alias{graph_widget}
\alias{graph_plot}
\alias{graph_save}
\alias{knit_print.ggx_graph}
\title{Use an interactive graph as a widget, a ggplot or a file}
\usage{
graph_widget(x, theme = c("auto", "light", "dark"))

graph_plot(x, theme = c("light", "dark"))

graph_save(x, file, res = 300, theme = NULL)

\method{knit_print}{ggx_graph}(x, ...)
}
\arguments{
\item{x}{A graph from \code{\link[=ggcausal]{ggcausal()}}, \code{\link[=ggnma]{ggnma()}}, \code{\link[=ggmeta]{ggmeta()}}, \code{\link[=ggfunnel]{ggfunnel()}},
\code{\link[=ggleague]{ggleague()}}, \code{\link[=ggkm]{ggkm()}}, \code{\link[=ggswimmer]{ggswimmer()}} or \code{\link[=ggchoropleth]{ggchoropleth()}}.}

\item{theme}{For the widget, \code{"auto"} follows the page it sits on: a dark
'pkgdown' or 'bslib' page (\code{data-bs-theme="dark"}), a dark Quarto theme,
a page marked \code{data-theme="dark"}, or, for a widget saved as its own
page, the viewer's system setting. \code{"light"} and \code{"dark"} fix it. For
\code{\link[=graph_plot]{graph_plot()}} and a PNG from \code{\link[=graph_save]{graph_save()}}, \code{"light"} or \code{"dark"}
picks the colors of the static copy; a saved \code{.html} defaults to
\code{"auto"} and a \code{.png} to \code{"light"}.}

\item{file}{Output path. \code{.html} writes the widget as a single file, which
needs 'pandoc'; \code{.png} writes a static image with 'ragg'.}

\item{res}{Resolution of a PNG in pixels per inch.}

\item{...}{Passed to \code{\link[knitr:knit_print]{knitr::knit_print()}}.}
}
\value{
\code{graph_widget()} returns an htmlwidget and \code{graph_plot()} a
ggplot. \code{graph_save()} returns \code{file}, invisibly.
}
\description{
A graph built by \code{\link[=ggcausal]{ggcausal()}}, \code{\link[=ggnma]{ggnma()}}, \code{\link[=ggmeta]{ggmeta()}}, \code{\link[=ggfunnel]{ggfunnel()}},
\code{\link[=ggleague]{ggleague()}}, \code{\link[=ggkm]{ggkm()}}, \code{\link[=ggswimmer]{ggswimmer()}} or \code{\link[=ggchoropleth]{ggchoropleth()}} prints as an
interactive widget. These
functions give the other forms it can take. \code{graph_widget()} returns the
'htmlwidgets' object, for use in 'shiny' or to save with
\code{\link[htmlwidgets:saveWidget]{htmlwidgets::saveWidget()}}. \code{graph_plot()} returns the underlying
ggplot, which draws as an ordinary static plot. \code{graph_save()} writes
either form to a file chosen by its extension.
}
\details{
The graph is laid out at a fixed size, so its boxes always fit their
labels. The widget scales to the width of the page it sits in; a static
copy should be drawn at \code{x$width} by \code{x$height} inches, which is what
\code{graph_save()} does. The widget embeds a web copy of Lato, so it looks the
same on machines without the font.

On a dark page the widget switches to a dark palette of its own: the
background, text, lines and neutral fills take their dark counterparts,
colors that carry meaning keep their hue, and the hover cards and panels
follow. It also follows a page that switches theme while it is open.
}
\examples{
dag <- ggcausal(cleft_dag$edges, cleft_dag$nodes)
w <- graph_widget(dag)
p <- graph_plot(dag)
\donttest{
graph_save(dag, tempfile(fileext = ".png"))
}
}
