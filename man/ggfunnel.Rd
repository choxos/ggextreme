% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta_funnel.R
\name{ggfunnel}
\alias{ggfunnel}
\title{Draw an interactive funnel plot for a meta-analysis}
\usage{
ggfunnel(
  x,
  data = NULL,
  rob = NULL,
  hover = NULL,
  contours = c(0.1, 0.05, 0.01),
  trim_fill = FALSE,
  tests = TRUE,
  exponentiate = NULL,
  xlim = NULL,
  xlab = NULL,
  title = NULL,
  caption = NULL,
  family = "Lato"
)
}
\arguments{
\item{x}{A fitted meta-analysis: an \code{rma.uni} object from
\code{\link[metafor:rma]{metafor::rma()}}, or a \code{meta} object from the 'meta' package, such as
the result of \code{\link[meta:metabin]{meta::metabin()}} or \code{\link[meta:metagen]{meta::metagen()}}. Models with
moderators are not supported.}

\item{data}{Optional data frame with one row per study, in the order of
the model, holding the columns to show. Defaults to the data stored in
the fit, which is there when the model was fitted with a \code{data}
argument.}

\item{rob}{Name of the column of \code{data} holding each study's overall risk
of bias judgment, which colors its point. Judgments are matched by
their wording, as in \code{\link[=ggmeta]{ggmeta()}}.}

\item{hover}{Names of columns of \code{data} shown in each study's hover card.
Defaults to \code{columns}.}

\item{contours}{Significance levels for the shaded contours, against no
effect. \code{NULL} draws none.}

\item{trim_fill}{Add the studies imputed by trim and fill and the adjusted
estimate.}

\item{tests}{Add the tests for small-study effects, in a collapsed section
under the plot.}

\item{exponentiate}{Show effects on the ratio scale. Defaults to \code{TRUE}
for ratio measures (\code{RR}, \code{OR}, \code{HR}, \code{IRR}, \code{ROM} and Peto odds
ratios), which are modeled on the log scale.}

\item{xlim}{Optional limits of the effect axis, on the scale shown.}

\item{xlab}{Axis label. Defaults to the name of the effect measure.}

\item{title, caption}{Title above the plot and note below it.}

\item{family}{Font family. The package ships Lato and registers it on load.}
}
\value{
An object of class \code{ggfunnel}, which prints as an interactive
widget. Use \code{\link[=graph_widget]{graph_widget()}}, \code{\link[=graph_plot]{graph_plot()}} or \code{\link[=graph_save]{graph_save()}} for the
widget, a static ggplot or a file. The field \code{tests} holds the tests as
a data frame.
}
\description{
Draws each study's effect against its standard error, with the most
precise studies at the top, to show small-study effects. The shaded
contours mark where a study would be statistically significant against
no effect (Peters et al. 2008), so a gap in the unshaded area, where
studies would not be significant, points to publication bias rather than
heterogeneity alone. The solid line is the pooled estimate and the dashed
lines around it the region where 95\% of studies would fall without
heterogeneity or bias.
}
\details{
Hovering over a study shows its effect, weight, risk of bias, the columns
named in \code{hover} and the significance zone it falls in. Clicking it opens
the pooled estimate with that study left out, beside its full record.

With \code{tests = TRUE}, a section under the plot, collapsed until the reader
opens it, gives Egger's regression test (Egger et al. 1997), Begg's rank
correlation test (Begg and Mazumdar 1994) and, with \code{trim_fill = TRUE},
the trim and fill estimate (Duval and Tweedie 2000), as computed by
'metafor' or 'meta', with a note on what each asks. Egger's test is the
classical one, \code{metafor::regtest(model = "lm")}, which 'meta' also
computes; metafor's own default, \code{regtest()} with \code{model = "rma"}, gives a
different p value. The two packages' trim and fill estimators can also
impute different numbers of studies from the same data. The table is also
returned as the field \code{tests}. These tests have little power with fewer
than ten studies, and asymmetry can come from heterogeneity, chance or the
quality of small studies as well as from publication bias.

With \code{trim_fill = TRUE}, the studies trim and fill imputes are drawn as
hollow circles and the adjusted estimate as a dashed line, and the widget
gets a switch that hides them.
}
\examples{
if (requireNamespace("metafor", quietly = TRUE)) {
  dat <- metafor::escalc(measure = "RR", ai = tpos, bi = tneg,
                         ci = cpos, di = cneg, data = metadat::dat.bcg,
                         slab = paste(author, year))
  fit <- metafor::rma(yi, vi, data = dat)
  f <- ggfunnel(fit, hover = "alloc", trim_fill = TRUE)
  f
  f$tests
}
}
