% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/km.R
\name{animate_km}
\alias{animate_km}
\title{Animate a Kaplan-Meier plot}
\usage{
animate_km(
  x,
  file = "km.gif",
  duration = 5,
  end_pause = 2,
  fps = 30,
  res = 150,
  loop = TRUE,
  cores = max(1L, parallel::detectCores() - 1L),
  quiet = FALSE
)
}
\arguments{
\item{x}{A plot from \code{\link[=ggkm]{ggkm()}}.}

\item{file}{Output path, ending in \code{.gif} or \code{.mp4}.}

\item{duration}{Seconds to draw the full follow-up.}

\item{end_pause}{Seconds to hold the final frame.}

\item{fps}{Frames per second.}

\item{res}{Output resolution in pixels per inch.}

\item{loop}{Loop the GIF. Ignored for video.}

\item{cores}{Number of cores to draw frames on.}

\item{quiet}{Suppress the progress bar.}
}
\value{
\code{file}, invisibly.
}
\description{
Draws the curves of a \code{\link[=ggkm]{ggkm()}} plot over follow-up, as though the trial
were being watched, and writes the result to a GIF or MP4. The numbers at
risk appear as each break is reached and the time is shown large behind
the curves.
}
\examples{
\donttest{
if (requireNamespace("survival", quietly = TRUE)) {
  colon <- subset(survival::colon, etype == 2)
  km <- ggkm(survival::Surv(time / 365.25, status) ~ rx, data = colon,
             xlab = "Years")
  animate_km(km, tempfile(fileext = ".gif"), duration = 2, fps = 8,
             cores = 1)
}
}
}
