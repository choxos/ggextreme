% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/cinema.R
\name{cinema_judge}
\alias{cinema_judge}
\title{Judge confidence in the results of a network meta-analysis}
\usage{
cinema_judge(
  x,
  rob = NULL,
  indirectness = NULL,
  reporting = NULL,
  threshold = NULL,
  rule = "average",
  judgments = NULL,
  small_values = NULL,
  order = NULL,
  pooled = NULL,
  contributions = NULL,
  split = NULL
)
}
\arguments{
\item{x}{A network meta-analysis from \code{\link[netmeta:netmeta]{netmeta::netmeta()}}.}

\item{rob, indirectness}{Study level judgments of risk of bias and of
indirectness: data frames with a column \code{study}, naming every study in
the network once, a column \code{judgment}, and optionally a column \code{reason}
in words. A judgment is low, moderate or high, written as \code{"low"},
\code{"some concerns"} (or \code{"moderate"} or \code{"unclear"}) and \code{"high"}, as
\code{"l"}, \code{"m"} and \code{"h"}, or as 1, 2 and 3, the codes the CINeMA web
application reads.}

\item{reporting}{Your judgment of reporting bias, which cannot be computed
from the data: a data frame with a column \code{judgment}, \code{"undetected"} or
\code{"suspected"} (\code{"strongly suspected"} is also read), an optional column
\code{reason}, and the comparison it applies to, either as \code{treat1} and
\code{treat2} or as \code{comparison} (such as \code{"A:B"} or \code{"A vs B"}). A data frame
with a single row and no comparison applies to every comparison.}

\item{threshold}{The limits of the range of little difference, on the
scale of the effect (an odds ratio, say, not its logarithm), for the
first treatment of each comparison against the second: one number, such
as \code{1.25}, for a range symmetric about no effect (0.8 to 1.25 here), or
two numbers for independent lower and upper limits, which must lie on
either side of no effect. A threshold of no effect itself, 1 for a ratio
or 0 for a difference, treats any effect as important. Imprecision and
heterogeneity, and incoherence when its test gives p of 0.10 or less,
need it.}

\item{rule}{How the study judgments are summarized for each comparison:
\code{"average"}, \code{"majority"} or \code{"highest"}. Give two, such as
\code{c("average", "highest")}, for different rules for within-study bias and
for indirectness. See the section on rules.}

\item{judgments}{Domain judgments you made yourself, such as those
exported from the CINeMA web application, which replace the computed
ones. Either wide, with the comparison (\code{treat1} and \code{treat2}, or
\code{comparison}) and one column per domain, named like the domains
(\code{"Within-study bias"}, \code{"Reporting bias"}, \code{"Indirectness"},
\code{"Imprecision"}, \code{"Heterogeneity"}, \code{"Incoherence"}), optional columns
such as \code{"Imprecision reason"} and optional \code{"Confidence rating"} and
\code{"Reason(s) for downgrading"} columns; or long, with the comparison and
columns \code{domain}, \code{judgment} and optionally \code{reason}. A missing or empty
judgment leaves the computed one in place.}

\item{small_values}{Whether small values of the effect are \code{"desirable"},
as for mortality, or \code{"undesirable"}, as for a response. It sets the
ranking, and so the order of the treatments, and which side of the range
is a benefit. Defaults to the setting stored in \code{x}, which is worth
checking.}

\item{order}{The order of the treatments. Each comparison is written with
the treatment that comes first in this order first, and the limits in
\code{threshold} apply in that direction. Defaults to the P-score ranking,
best first.}

\item{pooled}{Which model to use, \code{"random"} or \code{"common"}. Defaults to
the random effects model when \code{x} has one.}

\item{contributions}{The contribution of each study to each estimate: an
object from \code{netmeta::netcontrib(x, study = TRUE)}, or \code{TRUE} to compute
it. By default it is computed when \code{rob} or \code{indirectness} is given,
which takes a few seconds for a network of a few dozen studies.}

\item{split}{The direct and indirect estimates: an object from
\code{\link[netmeta:netsplit]{netmeta::netsplit()}}, or \code{NULL} to compute them with its default
method, back-calculation. Give one computed with \code{method = "SIDDE"} to
use that method instead.}
}
\value{
An object of class \code{cinema}, a list whose main fields are
\code{judgments}, one row per comparison and domain with the \code{level} (0, 1
or 2 for no, some or major concerns, \code{NA} when not judged), the
\code{judgment} in words, the \code{reason} and its \code{source}; \code{comparisons}, the
network, direct and indirect estimates, prediction intervals and
inconsistency factors on the scale of the effect; \code{contributions}, the
share of each estimate from each study; \code{studies}, the study judgments;
\code{threshold}, \code{rule}, \code{global} (the design by treatment test) and \code{fit}.
It prints as a table of the judgments.
}
\description{
Applies the rules of CINeMA, Confidence in Network Meta-Analysis
(Nikolakopoulou et al. 2020; Papakonstantinou et al. 2020), to every
comparison of a network meta-analysis. Each comparison gets a judgment of
no concerns, some concerns or major concerns in each of six domains:
within-study bias, reporting bias, indirectness, imprecision, heterogeneity
and incoherence, each with its reason in words and a note of whether it
was computed by a rule or given by you. The domains are kept side by side
and never added into a score.
}
\details{
The result feeds the plots of the family, so they share one set of
estimates, contributions and judgments: \code{\link[=cinema_contribution]{cinema_contribution()}},
\code{\link[=cinema_clinical]{cinema_clinical()}}, \code{\link[=cinema_incoherence]{cinema_incoherence()}} and \code{\link[=cinema_league]{cinema_league()}}. Each of
them also accepts a netmeta fit and the arguments of this function.
}
\section{Rules}{

Every rule below is the one CINeMA implements, as published; where the
papers leave a detail open the choice made here is stated.
\itemize{
\item \strong{Within-study bias} and \strong{indirectness} combine the study judgments
with the percentage contribution of each study to each estimate
(Papakonstantinou et al. 2018), from \code{\link[netmeta:netcontrib]{netmeta::netcontrib()}}. The
\emph{majority} rule takes the level with the largest total contribution,
the more serious level on a tie; the \emph{average} rule scores low 1,
moderate 2 and high 3, averages the scores weighted by contribution and
rounds, halves up; the \emph{highest} rule takes the most serious level among
the studies that contribute more than 0.0001 percent. Low, moderate and
high become no, some and major concerns.
\item \strong{Reporting bias} is your judgment; CINeMA suggests suspected or
undetected, and suspected is shown as some concerns.
\item \strong{Imprecision} compares the confidence interval with the range of
little difference. There are no concerns when the interval lies wholly
within the range, or wholly on the side of no effect that the point
estimate is on; some concerns when it crosses no effect but not the
limit on the other side; and major concerns when it passes that limit,
so that it holds important effects in both directions.
\item \strong{Heterogeneity} judges the prediction interval by the same rule. There
are no concerns when it reaches the same step as the confidence
interval, some concerns when it reaches one step further and major
concerns when it reaches two (Table 4 of Papakonstantinou et al. 2020;
this reproduces every scenario in Figure 3 of Nikolakopoulou et al.
2020). A common effect model has no prediction interval, so
heterogeneity is then not judged.
\item \strong{Incoherence}, for a comparison with direct and indirect evidence,
uses the test of the difference between them from \code{\link[netmeta:netsplit]{netmeta::netsplit()}}
(SIDE). With p above 0.10 there are no concerns. Otherwise the areas
below, within and above the range of little difference are compared:
when both confidence intervals reach the same areas there are no
concerns, when they differ in one area some concerns, and when they
differ in two or three major concerns. A comparison with only direct or
only indirect evidence cannot be tested locally, and takes its judgment
from the global design by treatment interaction test of
\code{\link[netmeta:decomp.design]{netmeta::decomp.design()}}: major concerns below 0.05, some from 0.05 to
0.10 and no concerns above; when the network has no closed loop, so the
test cannot be computed, major concerns. Both tests have low power.
}

CINeMA's authors stress that these rules are a starting point: the
reasons say what each rule saw, so a judgment can be revised by giving it
in \code{judgments}. CINeMA also leaves any overall rating to the reviewers;
this function gives none, though a rating you supply is kept and shown.
}

\section{Sources}{

Nikolakopoulou A, Higgins JPT, Papakonstantinou T, et al. CINeMA: an
approach for assessing confidence in the results of a network
meta-analysis. PLoS Medicine 2020;17(4):e1003082.
\doi{10.1371/journal.pmed.1003082}

Papakonstantinou T, Nikolakopoulou A, Higgins JPT, Egger M, Salanti G.
CINeMA: software for semiautomated assessment of the confidence in the
results of network meta-analysis. Campbell Systematic Reviews
2020;16:e1080. \doi{10.1002/cl2.1080}

Papakonstantinou T, Nikolakopoulou A, Rucker G, et al. Estimating the
contribution of studies in network meta-analysis: paths, flows and
streams. F1000Research 2018;7:610.
}

\examples{
\donttest{
if (requireNamespace("netmeta", quietly = TRUE) &&
    requireNamespace("meta", quietly = TRUE)) {
  pw <- meta::pairwise(treat = treatment, event = pasi75_r,
                       n = pasi75_n, studlab = study,
                       data = psoriasis_nma, sm = "OR")
  nma <- netmeta::netmeta(pw, common = FALSE)
  # Illustrative study judgments, invented for this example: they are
  # not published assessments of these trials.
  rob <- data.frame(
    study = c("CLEAR", "ERASURE", "FEATURE", "FIXTURE", "JUNCTURE"),
    judgment = c("high", "low", "some concerns", "low", "some concerns")
  )
  j <- cinema_judge(nma, rob = rob, threshold = 1.25,
                    small_values = "undesirable",
                    reporting = data.frame(judgment = "undetected"))
  j
  head(j$judgments)
}
}
}
