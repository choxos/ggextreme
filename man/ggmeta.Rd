% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/meta.R
\name{ggmeta}
\alias{ggmeta}
\title{Draw an interactive forest plot for a meta-analysis}
\usage{
ggmeta(
  x,
  data = NULL,
  columns = NULL,
  rob = NULL,
  hover = NULL,
  cumulative = FALSE,
  exponentiate = NULL,
  xlim = NULL,
  favors = NULL,
  xlab = NULL,
  title = NULL,
  caption = NULL,
  family = "Lato"
)
}
\arguments{
\item{x}{A fitted meta-analysis: an \code{rma.uni} object from
\code{\link[metafor:rma]{metafor::rma()}}, or a \code{meta} object from the 'meta' package, such as
the result of \code{\link[meta:metabin]{meta::metabin()}} or \code{\link[meta:metagen]{meta::metagen()}}. Models with
moderators are not supported.}

\item{data}{Optional data frame with one row per study, in the order of
the model, holding the columns to show. Defaults to the data stored in
the fit, which is there when the model was fitted with a \code{data}
argument.}

\item{columns}{Names of columns of \code{data} to print as text columns beside
the study labels, such as event counts. Name the vector to set the
column headers.}

\item{rob}{Names of columns of \code{data} holding risk of bias judgements,
one per domain, drawn as traffic lights. Name the vector to set the
column headers, such as \code{c(D1 = "rob.R", Overall = "rob.overall")}.
Judgements are matched by their wording: "low", "some concerns" or
"moderate", "unclear", "high" or "serious", "critical", and "no
information", in any case.}

\item{hover}{Names of columns of \code{data} shown in each study's hover card.
Defaults to \code{columns}.}

\item{cumulative}{Show the cumulative meta-analysis rather than the
individual studies.}

\item{exponentiate}{Show effects on the ratio scale. Defaults to \code{TRUE}
for ratio measures (\code{RR}, \code{OR}, \code{HR}, \code{IRR}, \code{ROM} and Peto odds
ratios), which are modeled on the log scale.}

\item{xlim}{Optional limits of the effect axis, on the scale shown.}

\item{favors}{Optional labels for the two sides of the null line, such as
\code{c("Favors treatment", "Favors control")}.}

\item{xlab}{Axis label. Defaults to the name of the effect measure.}

\item{title, caption}{Title above the plot and note below it.}

\item{family}{Font family. The package ships Lato and registers it on load.}
}
\value{
An object of class \code{ggmeta}, which prints as an interactive
widget. Use \code{\link[=graph_widget]{graph_widget()}}, \code{\link[=graph_plot]{graph_plot()}} or \code{\link[=graph_save]{graph_save()}} for the
widget, a static ggplot or a file, and \code{\link[=animate_meta]{animate_meta()}} for the
cumulative replay.
}
\description{
Draws the forest plot of a fitted meta-analysis, with the data behind
every study a hover and a click away. Hovering over a study shows its
effect, weight and the columns named in \code{hover}; clicking it opens its
full record, every column of \code{data}, under the plot. Hovering over the
pooled diamond shows the heterogeneity statistics and the prediction
interval. Risk of bias judgements, given in \code{rob}, are drawn as traffic
lights beside each study.
}
\details{
Square area follows the study's weight in the model. The diamond is the
pooled estimate with its confidence interval, and the line through it the
prediction interval for a random effects model. Ratio measures such as
risk, odds and hazard ratios are shown on a log axis. A confidence
interval that runs past the axis ends in an arrow.

With \code{cumulative = TRUE}, each row shows the pooled estimate from the
studies up to and including it, in the order of the data, so sort the
data by year before fitting the model for a cumulative meta-analysis
over time. \code{\link[=animate_meta]{animate_meta()}} replays that sequence as an animation.
}
\examples{
if (requireNamespace("metafor", quietly = TRUE)) {
  dat <- metafor::escalc(
    measure = "OR", ai = p2y12.mi, n1i = p2y12.total,
    ci = aspirin.mi, n2i = aspirin.total,
    data = metadat::dat.chiarito2020, slab = paste(study, year)
  )
  dat <- dat[!is.na(dat$yi), ]
  dat$p2y12 <- paste0(dat$p2y12.mi, "/", dat$p2y12.total)
  dat$aspirin <- paste0(dat$aspirin.mi, "/", dat$aspirin.total)
  fit <- metafor::rma(yi, vi, data = dat)

  ggmeta(
    fit,
    columns = c("P2Y12 inhibitor" = "p2y12", Aspirin = "aspirin"),
    rob = c(R = "rob.R", D = "rob.D", Mi = "rob.Mi", Me = "rob.Me",
            S = "rob.S", Overall = "rob.overall"),
    favors = c("Favors P2Y12 inhibitor", "Favors aspirin")
  )
}
}
