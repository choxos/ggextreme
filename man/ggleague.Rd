% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/league.R
\name{ggleague}
\alias{ggleague}
\title{Draw an interactive league table for a network meta-analysis}
\usage{
ggleague(
  x,
  data = NULL,
  study = NULL,
  treatment = NULL,
  pooled = NULL,
  small_values = NULL,
  order = NULL,
  ranking = TRUE,
  contributions = NULL,
  title = NULL,
  caption = NULL,
  family = "Lato"
)
}
\arguments{
\item{x}{A network meta-analysis from \code{\link[netmeta:netmeta]{netmeta::netmeta()}}.}

\item{data}{Optional arm level data, one row per study arm, for the click
panels, such as the data given to \code{\link[=ggnma]{ggnma()}}. Every column other than
\code{study} and \code{treatment} becomes a row of the arm table.}

\item{study, treatment}{Bare column names in \code{data} identifying the study
and treatment of each arm. Treatment names must match those in \code{x}.}

\item{pooled}{Which model to show, \code{"random"} or \code{"common"}. Defaults to
the random effects model when \code{x} has one.}

\item{small_values}{Whether small values of the effect are
\code{"desirable"}, as for mortality, or \code{"undesirable"}, as for a response.
Sets which treatment a cell favors and the direction of the ranking.
Defaults to the setting stored in \code{x}, which is worth checking.}

\item{order}{Order of the treatments along the diagonal. Defaults to the
ranking, best first.}

\item{ranking}{Draw the P-score ranking beside the table.}

\item{contributions}{Show where each network estimate comes from: \code{TRUE}
to compute the share of it that flows through each direct comparison
with \code{\link[netmeta:netcontrib]{netmeta::netcontrib()}}, or an object that function returned, which
saves recomputing it for a large network. Hovering or tapping a network
estimate then marks the direct comparisons it draws on with their
shares, and its panel lists them.}

\item{title, caption}{Title above the table and note below it. The default
caption explains which estimates sit on each side of the diagonal.}

\item{family}{Font family. The package ships Lato and registers it on load.}
}
\value{
An object of class \code{ggleague}, which prints as an interactive
widget. Use \code{\link[=graph_widget]{graph_widget()}}, \code{\link[=graph_plot]{graph_plot()}} or \code{\link[=graph_save]{graph_save()}} for the
widget, a static ggplot or a file. The fields \code{treatments} and \code{pscore}
give the order used and the P-scores.
}
\description{
Draws every pairwise estimate of a network meta-analysis as a grid, with
the treatments on the diagonal and a ranking beside it. Following
\code{\link[netmeta:netleague]{netmeta::netleague()}}, each cell compares the treatment that comes first
in the table with the one that comes second: network estimates sit below
the diagonal and direct estimates, from the trials that compare the pair
head to head, above it.
}
\details{
Cells are shaded by the size of the effect, in one color when it favors
the first treatment and another when it favors the second, and faded
when the confidence interval includes the null. Hovering over a cell
shows its network, direct and indirect estimates and the share of the
network estimate that comes from direct trials. Clicking a cell opens the
direct trials under the table: their arm level data when \code{data} is given,
and otherwise each trial's own estimate. Hovering over a treatment on the
diagonal or in the ranking lights its row and column.
}
\examples{
\donttest{
if (requireNamespace("netmeta", quietly = TRUE) &&
    requireNamespace("meta", quietly = TRUE)) {
  pw <- meta::pairwise(treat = treatment, event = pasi75_r,
                       n = pasi75_n, studlab = study,
                       data = psoriasis_nma, sm = "OR")
  nma <- netmeta::netmeta(pw, common = FALSE)
  ggleague(nma, psoriasis_nma, study, treatment,
           small_values = "undesirable")
}
}
}
