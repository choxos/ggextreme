% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/race.R
\name{race_size}
\alias{race_size}
\title{The pixel size of a race frame}
\usage{
race_size(x, width = x$width)
}
\arguments{
\item{x}{A \code{ggrace} object from \code{\link[=ggrace]{ggrace()}}.}

\item{width}{Output width in pixels. Defaults to the width the race was
built for.}
}
\value{
A named numeric vector with the \code{width} and \code{height} in pixels.
}
\description{
A frame has a fixed aspect ratio, set by the width and by how many bars are
visible. \code{\link[=animate_race]{animate_race()}} uses this to size its device; use it when drawing
a single frame yourself, so the layout is not stretched.
}
\examples{
race <- ggrace(clefts_qci, qci, country, year, top_n = 10)
race_size(race)
}
