% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/race.R
\name{race_frame}
\alias{race_frame}
\title{Draw one frame of a bar chart race}
\usage{
race_frame(x, frame = 1L)
}
\arguments{
\item{x}{A \code{ggrace} object from \code{\link[=ggrace]{ggrace()}}.}

\item{frame}{Frame index, between 1 and the number of frames.}
}
\value{
A \link[ggplot2:ggplot]{ggplot2::ggplot} object.
}
\description{
The frame is a single \link[ggplot2:ggplot]{ggplot2::ggplot} laid out in card units, so the
title, axis, bars, timeline and footer all live in one coordinate system
and land on measured positions rather than wherever a layout engine puts
them. That is what keeps the panel from shifting sideways between frames.
}
\details{
Frames are set in Lato, which the package registers with 'systemfonts'.
Draw them on a device that understands registered fonts, such as
\code{\link[ragg:agg_png]{ragg::agg_png()}}, which is what \code{\link[=animate_race]{animate_race()}} uses. On other devices
pass \code{family = ""} to \code{\link[=ggrace]{ggrace()}} to fall back to the device default.
}
\examples{
phones <- as.data.frame.table(datasets::WorldPhones, responseName = "phones")
names(phones)[1:2] <- c("year", "region")
phones$year <- as.numeric(as.character(phones$year))

race <- ggrace(phones, phones, region, year, top_n = 7, family = "")
race_frame(race, 1)
}
