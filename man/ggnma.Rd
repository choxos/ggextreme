% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/network.R
\name{ggnma}
\alias{ggnma}
\title{Draw an interactive network plot for a network meta-analysis}
\usage{
ggnma(
  data,
  study,
  treatment,
  n = NULL,
  group = NULL,
  hover = NULL,
  multiarm = TRUE,
  positions = NULL,
  palette = NULL,
  legend = TRUE,
  legend_title = NULL,
  title = NULL,
  caption = NULL,
  family = "Lato"
)
}
\arguments{
\item{data}{A data frame with one row per study arm.}

\item{study, treatment}{Bare column names identifying the study and the
treatment of each arm. Each treatment may appear once per study.}

\item{n}{Optional bare column giving the number of participants in each
arm. Sets node area and the participant counts in the hover cards.}

\item{group}{Optional bare column naming a class for each treatment, such
as a drug class. Nodes are then colored by class and a legend is drawn.
Each treatment must belong to exactly one class.}

\item{hover}{Names of the columns shown in the hover card, one row each,
such as the sample size, an outcome and a key baseline characteristic.
Defaults to the first four columns of the arm table. Use
\code{character(0)} for a card that only lists the studies. The click panel
always shows every column.}

\item{multiarm}{Shade a polygon for each set of treatments compared in a
study with more than two arms.}

\item{positions}{Optional data frame placing the nodes by hand, with
columns \code{treatment}, \code{x} and \code{y}, one row per treatment and \code{y} pointing
up. Any units will do: the layout is scaled to fit the plot, keeping its
shape. Labels point away from the middle of the layout.}

\item{palette}{Node colors. With \code{group}, a vector named by class, or an
unnamed vector recycled over the classes; without it, a single color.
Defaults to \code{\link[=race_palette]{race_palette()}}.}

\item{legend}{Draw the legend when \code{group} is given.}

\item{legend_title}{Text in front of the legend, such as \code{"Class"}.}

\item{title, caption}{Title above the plot and note below it.}

\item{family}{Font family. The package ships Lato and registers it on load.}
}
\value{
An object of class \code{ggnma}, which prints as an interactive widget.
Use \code{\link[=graph_widget]{graph_widget()}}, \code{\link[=graph_plot]{graph_plot()}} or \code{\link[=graph_save]{graph_save()}} for the widget, a
static ggplot or a file. The fields \code{nodes} and \code{edges} hold the
treatments and comparisons with their study counts, \code{multiarm} the sets
of treatments compared in multi-arm studies, and \code{width} and \code{height}
the natural size in inches.
}
\description{
Draws the network of treatment comparisons from arm level data. Each node
is a treatment and each line joins two treatments compared directly in at
least one study. Hovering over a node shows a compact table of every arm
on that treatment; hovering over a line shows the arms of each study that
makes that comparison. The card holds the columns named in \code{hover}.
Clicking either opens a panel under the plot with the full arm level data
side by side, one column per arm, in the manner of a trial's baseline
table: every column of \code{data} other than \code{study}, \code{treatment} and \code{group}
becomes a row, so baseline characteristics and outcomes are shown as they
were given.
}
\details{
Treatments sit on a circle, starting at the top and running clockwise in
the order of the levels of \code{treatment} when it is a factor, or in order of
first appearance otherwise; \code{positions} places them by hand instead. Line
width follows the number of studies that make the comparison. When \code{n} is
given, node area follows the total number of participants on that
treatment. A study with more than two arms adds a line for every pair of
its treatments and, with \code{multiarm}, a shaded polygon joining them.
Studies that compare the same set of treatments share one polygon, which
has its own hover card and panel.

Row labels come from each column's \code{label} attribute when it has one, as
set by the 'labelled', 'Hmisc' or 'haven' packages, and otherwise from its
name. Text that contains a URL or a DOI, such as a reference column, is
linked in the panel.
}
\examples{
net <- ggnma(psoriasis_nma, study, treatment, n = n, group = class,
             legend_title = "Class")
net
net$edges
}
